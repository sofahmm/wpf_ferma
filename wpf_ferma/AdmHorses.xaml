<Page x:Class="wpf_ferma.AdmHorses"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpf_ferma"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdmHorses">

    <Grid>
        <!--<ListView x:Name="Horses" ItemsSource="{Binding Horse}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
        
        <DataGrid x:Name="Table" AutoGenerateColumns="True" ItemsSource="{Binding Horse}"></DataGrid>
        <Grid  Height="50" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
            <Button Width="100"  Content="Добавить"/>
        </Grid>
    </Grid>
</Page>
